* make proper exclusive/mute groups  (and auto if not defined at the kit file)

* strange click in multi-layered samples when fast triggering

* add "humanize" via pitch shifting/light dist/etc
* add selectable drummaps //as in Cubase, but n1=n2 format



